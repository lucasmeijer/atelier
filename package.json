{
  "name": "atelier",
  "private": true,
  "type": "module",
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ],
    "catalog": {
      "@earendil-works/pi-ai": "^0.79.10",
      "@earendil-works/pi-coding-agent": "^0.79.10"
    }
  },
  "scripts": {
    "generate:workspace-modules": "bun run --cwd apps/web scripts/generate-workspace-modules.ts",
    "check": "bun run generate:workspace-modules && bunx tsc --noEmit -p tsconfig.json",
    "test": "bun run generate:workspace-modules && bun test",
    "ci": "bun ci && bun run check && bun run test",
    "web": "bun run --cwd apps/web dev",
    "smoke:vscode": "node apps/web/smoke/vscode-smoke.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.61.0",
    "bun-types": "1.3.14",
    "typescript": "6.0.3"
  }
}
