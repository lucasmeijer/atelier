{
  "name": "@atelier/web",
  "private": true,
  "type": "module",
  "scripts": {
    "build:client": "bun build src/client/workspace.ts --outdir public --format esm --target browser",
    "dev": "bun run build:client && bun run src/server/main.ts"
  },
  "dependencies": {
    "@atelier/agent": "workspace:*",
    "@atelier/browser": "workspace:*",
    "@atelier/core": "workspace:*",
    "@atelier/pi-config": "workspace:*",
    "@atelier/shared": "workspace:*",
    "@atelier/terminal": "workspace:*",
    "@atelier/vscode": "workspace:*",
    "@atelier/workspace-proxy": "workspace:*"
  }
}
